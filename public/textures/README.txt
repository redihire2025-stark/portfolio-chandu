Drop the two Earth images here with these exact filenames:

  earth-day.jpg    - daytime satellite map
  earth-night.jpg  - night lights map

The globe in the Contact section loads them automatically at these paths:
  /textures/earth-day.jpg
  /textures/earth-night.jpg

Until both files exist, the globe falls back to a generated placeholder
texture so the page still renders correctly.
